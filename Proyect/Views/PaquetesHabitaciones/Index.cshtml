@model IEnumerable<Proyect.Models.PaquetesHabitacione>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Precio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdHabitacionNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPaqueteNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Precio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdHabitacionNavigation.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPaqueteNavigation.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPaqueteHabitacion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPaqueteHabitacion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPaqueteHabitacion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
